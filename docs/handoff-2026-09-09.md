# Handoff — 2026-09-09

The current handoff. Ten superseded records — five older handoffs, an older audit, two backlog
surveys, a todo re-check and the 2026-08-27 evolution-loop note — were deleted on 2026-09-09;
everything from them that is still true is here, in `TODO.md`, in the two design docs beside this
file, or inlined at the code that used to cite them.

---

## 1 · The state

Every row is a command re-run on `45294b4` for this document.

| fact | value | command |
|---|---|---|
| tests | **3,611 pass / 0 fail** (suites 0, cancelled 0, skipped 0, todo 0) | `npm test` |
| typecheck | exit 0 | `npx tsc -p packages/core/tsconfig.test.json` |
| kernel | 10 files pinned, **12 declared seams over the full history**; notice: 36 pre-`since` feat commits grandfathered | `node scripts/check-kernel.mjs` |
| zero runtime deps | ok, **66 files scanned** | `node scripts/check-zero-dep.mjs` |
| pinned exports | NOT re-run — the guard reads `packages/core/dist/index.d.ts` and exits 1 naming `npm run build` when it is absent, which it is in a fresh worktree. A STALE `dist/` passes. | `npm run build && node scripts/check-surface.mjs` |
| NUL census | **5** tracked files carry a NUL byte, **0** invalid UTF-8, of **456** tracked — the denominator moves with every add or delete, so re-run it rather than carrying it | read every `git ls-files` path, `Buffer.includes(0)` and a `new TextDecoder("utf-8",{fatal:true})` decode |
| `§A0` rows | **17** rows, **9** struck, **8** open | `/usr/bin/grep -acE '^- (~~)?\*\*A0\.[0-9]+ ' TODO.md`, then the `~~` and the un-struck variants |

`npm run check` (`typecheck && test && zero-dep && surface && kernel`) was NOT run here — it does a
`tsc -b --force` that races on emit when other agents are working. Run it once, alone, before a
merge; use the read-only typecheck otherwise.

**The kernel guard's "N commits judged" figure moves with every commit** and is not comparable
across documents. Carry the seam count, which moves only when somebody declares one.

**Three commands give three seam counts, and each difference names a real limit.**
`node scripts/check-kernel.mjs | head -1` → **12**; `git log --grep='^Kernel-seam:' --oneline |
wc -l` → **13** (the extra is `2a9eda8`, a `docs:` commit quoting the trailer in prose and touching
only `TODO.md`); git's own trailer parser reads only a final paragraph and sees **7**. CLAUDE.md §1
has the full paragraph.

The five NUL files: `src/evolution/trajectory.ts`, `src/journal/payloads.ts`,
`test/builtin/fs-search.test.ts`, `test/run/delivery.test.ts`, `test/server/http.test.ts`.

---

## 2 · What is open

`TODO.md` is the authority; every item below is a row there, so nobody has to read a gitignored lane
report to find one.

- **A0.12** — a stranded run recompiles the workspace every tick; needs a cache-invalidation design.
- **A0.13** — the usage-floor's ~10× rate ratio. The ~80× compounding is closed; the residue is
  structural, not unfinished.
- **A0.22** — the delegation door, which cannot close ahead of **§D.6** (the decision itself).
- **A0.23** — the seventh cross-run touch, `this.advance(childRunId)` at `engine.ts:8174`, still
  `E_INTERNAL`.
- **A0.24** — `gates.ts` keeps its idempotency entry after a non-`E_SEQ_CONFLICT` throw, which makes
  site D's retry inert in-process.
- **A0.25** — the `E_SUBGRAPH_FAILED` split; needs a `CODES` member.
- **A0.26** — a clean merge's `Kernel-seam:` trailer is invisible to a path-filtered census, and the
  feat path's regex is loose.
- **A0.27** — overlapping-prefix tool reservation; every `ToolDefinition` field except `name` is read
  live off the caller.
- **§A.29** — a frozen golden case pins channel bytes, so an honest fix the verifier certifies is
  refused by `1-must-pass` at 33.3pp. Untouched by the exam design; alternative G in
  `design-property3-2026-09-05.md` is the answer to the defect itself.

**Two more with a live code site and no row**, both in `server/http.ts` and both pre-existing: a
concurrent same-key submission INHERITS the refusal, because `send(res, 202, await inflight)`
(`:3449`) has no `try` — a diff reviewer *"tried five times to construct the interleaving and could
not"*; and `truncate` (`:4804`) bounds length without sanitising control characters, where the
newer `clip` does both. Every other sub-row lane residue lives only in the gitignored
`.agent/<lane>/report.md` of the wave that found it, and dies with that directory.

Deliberately not rows, and recorded so they are not rediscovered as defects:
`#uncompensatedIrreversible` refusing with the raw store error is correct (a refusal guard's
undecidable case must fail closed); `#resolveOnce`'s seen-key map and the broker's idempotency map
are in memory, so a repeat redelivery answers 200 in the deciding process and 409 after a restart —
it fails CLOSED and the journal is identical either way, so it is not a tenth journal violation, and
closing it means journaling idempotency keys, which is a kernel `feat` with a seam argument.

---

## 3 · Decisions taken without the user, still in force

- **Merges run from the main checkout by a separate merge agent**, never from inside a lane
  worktree, and every merge re-verifies `git merge-base <target> <branch>` rather than trusting the
  lane.
- **A read counts as a read only where the executor unconditionally evaluates it** — node-level
  `reads`, `${…}` in tool args, a fanout edge's `over`. **Edge conditions never count**, on
  CLAUDE.md's "when a guard cannot decide, it fails closed". Cost, accepted: an exam whose only read
  is in a `when` is refused and the operator must add a real read.
- **Process hygiene, now a standing lane rule.** No unbounded `until … sleep` loops (macOS zsh has
  no `timeout`); every `node --test` carries `--test-timeout=<ms>`; every background process a lane
  starts is killed by that lane before its final message; reviewers work read-only in the lane's
  worktree rather than in one of their own.
- **A lane's report is written to disk BEFORE its final message**, so a lane that dies is still
  legible. Where the harness refuses the write, the final message is the report and somebody else
  persists it.

---

## 4 · Property 3

Unchanged since `ec2ad88`, and that was checked rather than assumed on the last wave: no lane edited
`evolution/`, `exam`-anything, or `journal/events.ts`. The claim, the five assumptions it holds
under, and the `names-only.json` gap are stated in `CLAUDE.md` §3; the design and its post-implementation
corrections are `docs/design-property3-2026-09-05.md`.

---

## 5 · Facts you would otherwise rediscover

- **`npm test`'s count is not the number of tests that ran.** A file whose bodies hijack
  `process.stdout.write` can report `ℹ tests 1` with the first test neither passed nor failed.
- **macOS zsh has no `timeout`.** `timeout 60 …` produces empty output that reads as a hang.
- **SIGTERM is deferred while `zsh` sits in `sleep`**, so `kill` looks like it did nothing; use
  `kill -9`. A loop you cannot reliably signal is a loop you cannot reliably stop.
- **`isolation: "worktree"` branches from the repository's DEFAULT branch, not from `HEAD`.** The
  symptom is a worktree with a pre-rewrite tree — `src/`, `tui/` — and no `packages/core/`. Verify
  your merge-base before you commit.
- **`git worktree remove` needs `git worktree unlock` first** when a live `claude` pid holds the
  lock ("cannot remove a locked working tree, lock reason: claude agent … (pid …)"). That pid is the
  process that spawned the lane agents, not a running build, so unlocking is safe once the lane is
  merged and verified.
- **Copy a lane's `.agent/<lane>/` out before removing its worktree** — `git worktree remove`
  deletes it.
- **`git merge` run from inside a worktree merges into THAT worktree's branch.**
- **`git archive <sha> | tar -x -C <dir>` is the cheap base proof**, one extraction per lane.
- **`git branch -a` marks a branch checked out in another worktree with `+`.** `+ init` is a second
  checkout, not a merge state.
- **A clean merge shows no files.** `git show --name-only --format='' <clean-merge>` prints nothing.
  Any tool that filters commits by "did it touch path P" is blind to clean merges — that is A0.26.
- **The binary refuses after source edits, and `npm run check` does not test that.**
  `scripts/verify-binary.mjs` is a CI `binary` job step; `check` includes neither it nor
  `build:binary`.
- **Drive a guard's TRUE arm before you defend it.** Substituting `if (false) throw e` for a guard
  and finding every test still green is how four rounds of argument became a deletion.
- **A row's stated mechanism is a hypothesis** — reproduce it before you cite it in the fix. One
  row's named mechanism (`plans['valueOf']`) was unreachable; the real defect was one object over.
- **A lane's `STATUS: CLOSED` is about the lane, never about its `TODO.md` row.**
