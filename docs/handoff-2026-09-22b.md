# Handoff — 2026-09-22b

The current handoff. It supersedes `docs/handoff-2026-09-22.md`, which covered the wave from
`6fb2e618` to `96a84086`; that file is left in place because other documents cite it, and everything
in it this wave did not move is still true — **with four exceptions, named here rather than edited
there.** (1) Its §2 says §A.62's grep *"keeps measuring something true: 6/4 → **4/4**"*, and its
Repros section pastes `packages/core/src/run/engine.ts:4  # unmoved, deliberately`. It reads **4/6**
now: §A.81(b) added `readableLoopBound` beside `readableFanoutWidth`, by that row's own argument, so
there are two hand-written copies of one predicate. The note is on §A.62's row. (2) Its §3 says
§A.80 is *"PRE-EXISTING, and pinned NEGATIVELY"* — the pin is POSITIVE now; a child's `kind: 42` is
refused. (3) Its §2 says `GRAPH004_EXPR` is total over `when` for a number, a `null` and an object
*"and for an ARRAY the compiler never returns at all"* — it returns `GRAPH004_EXPR`. (4) Its §5 says
a worktree was handed out based on `init` *"for the THIRD wave running"*; it is the FOURTH, and this
time all three were, at a commit that is not even a `loom` commit.

Its §1 kernel cell says **853/855** commits judged; the guard now prints **879**. That is not a
correction — the number moves with every commit, which is why `TODO.md` deliberately does not carry
it as a cell, and it is the convention 22 used for 19, 19 for 18, and 18 for 15b.

This one covers the **three-lane wave from `be29cb43` to `86a35a0a`**, merged in this order:
`452b7c9c` validate-a84 (§A.79, §A.80, §A.84, §A.81a), `5af927ee` bounds-a78-a81 (§A.78, §A.81b),
`86a35a0a` port-3 (the third workflow port). Three `--no-ff` merges, **zero conflicts** — the lanes
were file-ownership-disjoint (`graph/validate.ts` + `compile.ts` + `spec.ts`; `graph/expr.ts` +
`run/engine.ts`; `examples/**` + a new suite) and that has now held for **eight waves running**.

**The wave's shape in one sentence:** all three first cuts carried a confirmed BLOCKING defect an
independently-dispatched reviewer found from a `git archive` extract, and the sharpest of them was
not a missing check but a WRITTEN ARGUMENT that a new relation was safe — *"the cut can only
withhold a refusal it was already free to withhold"* — which a permutation fuzz refuted in one run,
after the compiler had stopped reporting a real race.

**§§1–5 are this wave.** **§6 is not** — it is the orchestrator's assessment of where the project
stands and what comes next, added on 2026-09-22 at the maintainer's request, with its numbers
re-measured against `29c8b9ec` rather than carried from a lane. It is marked as such at its head, so
a later reader does not read a judgement as a measurement. `DESIGN.md`'s Sequence items 29–31, and
the dated amendment to its item 24, are what it produced.

**Five rows closed against ten opened, and the open count ROSE for the second settlement running,
again by five.** That is what a port does, and this is the second consecutive receipt: five of the
ten new rows are the third port's friction log, met by a stranger driving the shipped binary. The
other five are residue of the four compiler rows this wave closed — found by probing what the
closures now compute, which is the other half of the same argument.

---

## 1 · The state

Every cell re-run on the settlement HEAD, with the command beside it. `TODO.md`'s "State" table
carries the same numbers and is the authority; this table is the one somebody reads first.

| fact | value | command |
|---|---|---|
| the gate | **exit 0** | `npm run check` |
| tests | **4,117 pass / 0 fail** (suites 0, cancelled 0, skipped 0, todo 0; 4,043 → 4,117) | `npm test` |
| typecheck | exit 0 | `npx tsc -p packages/core/tsconfig.test.json` |
| per-directory | `test/run` **1,469**, `test/graph` **515**, `test/server` **259**, `test/cli` **352**, `test/journal` **146**, `test/builtin` **94**, `test/evolution` **230**, `test/examples-*` **79** (of which `examples-grant` **26**, `examples-harden` **23**) | `node --test "packages/core/test/<dir>/**/*.test.ts"` |
| pinned exports | **542**, **unmoved** — and this is the wave where that guard was SEEN working rather than merely passing: the validate lane's first cut exported `BlockFieldType` and `check-surface.mjs` refused it (`added: BlockFieldType`), so the union is module-private and derived by an indexed-access type. `blockFieldTypes`, `readableLoopBound`, `computeDominators`, `flowAncestors` and `DomTree` are module-private too. **The blind spot is the same one, one table over**: `POLICY_FIELDS` and `NESTED_FIELDS` went ARRAY → RECORD and kept their names | `node scripts/check-surface.mjs` |
| kernel | 10 files pinned, **16 declared seams**, unmoved; **879** commits judged since `86b84c9` at `86a35a0a`, **881** on the settlement HEAD (853 last wave) — it counts this file's own commits, which is why it is not a cell in `TODO.md`; notice: 36 pre-`since` feat commits grandfathered. Kernel files touched: `graph/spec.ts` (3 commits: `d1b452fe`, `83aa54bf`, `79cab047`) and `run/engine.ts` (3: `648cc9c6`, `78e64ce3`, `297d12e0`), all `fix:`, none owing a trailer. `graph/validate.ts`, where three of the four closures landed, is on `kernel.json`'s `notKernel` list BY NAME | `node scripts/check-kernel.mjs` |
| zero runtime deps | ok, **67 files scanned**, unmoved — this wave added no `src/` file. Of its 27 new files, six are test suites, one is this port's doc, and twenty are a graph, a policy, thirteen request fixtures and five `function` bodies under `examples/`. Eleven files were modified, six of them `src/` | `node scripts/check-zero-dep.mjs` |
| NUL census | **5** tracked files carry a NUL byte, **0** are invalid UTF-8, out of **554** tracked at `86a35a0a` and **555** on the settlement HEAD, which adds this file (527 last wave). **Re-run because it had to be**: `docs/workflow-port-2026-09-22b.md` quotes control characters in prose, which is what created a sixth NUL file inside the 2026-09-19 wave — it wrote none, exactly as port 2's doc wrote none. The denominator is a measurement, not an invariant | read every `git ls-files` path, `Buffer.includes(0)` and a fatal `TextDecoder` |
| journal vocabulary | **51** event types, **unmoved** | `EVENT_TYPES.length`, asserted in `test/journal/store.test.ts` |
| error vocabulary | **60** codes, **unmoved** — measured, then said: §A.78 reuses `GRAPH004_EXPR`, §A.79 `GRAPH003_MALFORMED`, §A.80 `GRAPH003_UNKNOWN_EDGE_KIND`, §A.81(b) `E_GRAPH_INVALID`, §A.84 the four rule codes it made correct, and the port added none because it changed no source | `Object.keys(CODES).length` |
| README's extensibility ledger | **18** and **3**, both unchanged | the two `sed … \| /usr/bin/grep -a -c` commands in `CLAUDE.md` §2 |
| README's test floor | **3,500+**, unchanged; the probe counts **3,855** `test(` declarations across **363** files (3,781 / 357 last wave) | the Gates row of `README.md`, and `readme-gaps.test.ts`'s own probe |
| `TODO.md` rows | **156** rows, **100** struck, **56** open (**146 / 95 / 51** at `be29cb43`); §A went **83/56/27 → 93/61/32** | the three commands in `TODO.md`'s "Row census" |

**The third port drives end to end on the shipped binary, re-run here rather than trusted** — and it
is the first shipped graph that reaches THREE different endings, so all three are driven:

`loom compile graphs/grant-access.json` → `ok`, exit 0, **no diagnostic at all**;
`loom run … orders-db-backfill.json` → `awaiting_gate` and `out/` does not exist;
`loom gates $RUN` → `{"gate":"gate_…","approvers":["u:you"],"owners":["u:ravi","u:mina"]}`;
`loom approve $RUN $GATE --as u:you` → exit 0, `out/access-ledger.json` and `out/grant.json` written;
`loom replay $RUN` → `{"match": true, "hermetic": true}`;
`loom run … docs-site-read.json` → `succeeded`, `ceremony: "auto"`, **no gate at all**, both files
written; `loom run … orders-db-too-long.json` → `failed`, `E_FUNCTION_REFUSED`, **exit 1**. No
`--extension-module`, no fork, nothing under `packages/core/src` changed by that lane.

**And `harden-config.json` compiles SILENT now** — three warnings at `be29cb43`, zero at
`86a35a0a`, all three §A.84's — which is the only change across the seven shipped graphs'
`loom compile` output. `examples/README.md`'s two claims that it prints three warnings on every
command were corrected in this settlement's `docs(todo):` commit, along with README's examples
enumeration, which said §§1–4 and §8 where there are now ten sections and three ported workflows.

`npm test` still leaves no `x/` and no `.loom/` at the repo root. Two pieces of litter there still
PREDATE these waves and are still left alone: empty `graphs/` and `resources/` (27 Aug). This
settlement's own repro runs created `examples/.loom` and `examples/out`; both were deleted before
the commits, as the previous settlement deleted `examples/.loom` for the same reason.

---

## 2 · What closed

Five rows, and the shas are the argument. The per-row accounts below name the BLOCKING finding each
lane's first cut carried, because that is the part a green suite did not say.

| row | sha(s) | how |
|---|---|---|
| **A.78** | `931f844f`, `34410c2c` (+ pins `78e64ce3`, `297d12e0`) | `lex` type-tests its source AND asserts progress (`i <= lastStart`). `test/graph` **515/515** |
| **A.79** | `7dd6029c` (+ `79cab047`) | every required sub-block `rule016Subgraphs` reads is asked about before it is read, `GRAPH003_MALFORMED` naming the node |
| **A.80** | `5a0d1637` (+ `0431ddcb`) | the kind check moved beside `GRAPH020` inside `validateGraph`, so it reaches a child at any depth |
| **A.81** | `d1b452fe`, `83aa54bf` (a) · `648cc9c6`, `78e64ce3` (b) | **both halves**: a type per field in `POLICY_FIELDS`/`NESTED_FIELDS`, and a third `#assertBound` check for `maxIterations`. `test/run` **1,469/1,469** |
| **A.84** | `731eca44`, `79cab047` (+ `0431ddcb`) | `GRAPH010` rests on DOMINANCE, `GRAPH005`/`GRAPH002` on `flowAncestors` through `canPrecede`, over an edge set that KEEPS the back-edge |

### §A.78 — the guard is a progress assertion, and the first one was a stall test

The row asked for a progress assertion rather than a type test, *"because the defect is `i` not
advancing and any future branch that fails to advance it has the same shape"*. What shipped is
BOTH: a type test at the door (`E_EXPR_INVALID` → `GRAPH004_EXPR`,
`expression must be a string, got an array`) and `i <= lastStart` inside the loop. In `lex` and not
in `checkExpr`, because `parseExpr` is the EXECUTOR's own entrance and a `RunGraph` reaches it
without passing this build's compiler — the same argument §A.62 closed at two on.

**THE BLOCKING FINDING: the first cut asserted `i === lastStart`, which is a STALL test.** The
reviewer injected a branch that moves `i` BACKWARDS (`i = (i + op.length) % 2`), and the compiler
OOMed again with the guard in place. Worse, the lane's own round-1 pin was a REGEX asserting the
source reads `i === lastStart`, so the pin would have FORBIDDEN the strengthening. **Pin the
property, never the operator** — a sentence that now has a receipt.

**Both mutation pins run in a CHILD process**, 200 MB heap and a 30 s ceiling, and that is round 2's
own finding about round 1: an in-process pin under a `i <` weakening took the test runner's whole
heap, the file was charged as ONE unnamed failure although seven tests had reported, and the
`finally` that removes the temp directory never ran — leaked `loom-expr-*` directories were the
visible symptom. Re-measured after: the weakening is ONE named red in 753 ms, seven tests reported,
zero leaked directories.

**A deliberate TIGHTENING, recorded because it is not a fix.** A `String` OBJECT and a `String`
subclass PARSED at `be29cb43` and are refused now; `["a"]` and `["1"]` moved from a parse error to
`GRAPH004_EXPR`. None is reachable from a graph FILE — `JSON.parse` produces no boxed string —
and it is in the row so nobody reads the change as accidental.

### §A.79 — every sub-block, because one `?? {}` closes one input and leaves its siblings

The row's closing condition was the whole of the work: `inputs`, `outputs`, the `subgraph` block
itself, the child's `channels` and the child spec's shape are each asked about before they are read,
each answering `GRAPH003_MALFORMED` and NAMING the node, with a child's faults re-tagged
`in subgraph "…":` at the parent's node coordinate and verified three levels deep. The census that
made it a rule rather than an edit: `<block>: null` crashed the compiler on **all eight node types**
at the base, not only on `subgraph`.

**THE BLOCKING FINDING: a child `channels: []` compiled CLEAN where the base refused `GRAPH016`.**
The new "is this an object" question answered `[]` with *malformed, carry on*, and the parent then
read the child as declaring channels it does not have — a guard that made an ALREADY-REFUSED graph
QUIETER, which is the fail-open direction and the lens `CLAUDE.md` names. Fixed in two places: the
child's own `checkStructure` refuses an ARRAY, and the parent treats a non-object as *declares no
channels*.

### §A.80 — the relocation the docstring proposed, and what moved with it

`EDGE_KINDS` and the kind check left `compile.ts` and sit beside `GRAPH020` in `checkStructure`, so
the one refusal that is total over `kind` now reaches a child at any depth; the negative pin flipped
positive. **The message TEXT is byte-identical and its POSITION is not**, and both are disclosed at
the site: `GRAPH020_UNKNOWN_FIELD` now precedes `GRAPH003_UNKNOWN_EDGE_KIND` on a graph drawing
both, and the refusal is LOST behind an earlier fatal on a graph that has one. It stays non-fatal.

`renderKind` was made total on the way past — a `bigint`, a circular object, a throwing `toJSON` and
a `Proxy` all render, and `KIND_STILL_THROWS` is now empty of VALUES. A throwing ACCESSOR on the
edge object still throws at the property read, before any renderer sees it; that is base extent and
is a note on the row rather than a row. The third round added the one pin the reviewer asked for:
the `typeof` test must precede `Object.hasOwn`, because `[Symbol()]` throws at the membership test.

### §A.81 — both halves, and the enumerations that still said TWO

**(a)** `POLICY_FIELDS`/`NESTED_FIELDS` carry a type per field and one `blockFieldTypes` pass at 15
call sites enforces it: **21 fields newly refuse a wrong type and no in-tree graph tightened.**
`BlockFieldType` is module-private after `check-surface.mjs` refused the export, derived by an
indexed-access type with exhaustiveness proved by deleting an arm (TS2741). Three deliberate
non-tags, each with its reason in place: `metadata.version` and `channel.initial` are `unknown`
(§A.87, §A.88), and `policy.expansion` keeps `isPositiveInt` because a `count` tag would refuse LESS
than the bound already does. The cost, disclosed: `policy.capabilities`' refusal is FATAL, so
`GRAPH011_UNHANDLED_IRREVERSIBLE` is suppressed on a `k8s.apply` node in the same graph.

**(b)** `readableLoopBound` sits beside `readableFanoutWidth` and `#assertBound` gained a THIRD
vocabulary check, under `E_GRAPH_INVALID`, so it joins `#failUnreadableGraph` by code. The numeric
string `"6"` is refused too — the compiler already refuses it — and so is a restart. `maxIterations`
has **two readers**: `#loopMayContinue`, and `nodeShapeOf`, which copies it onto `ctx.node.out` for
every function body, which is the reader a first cut of the docstring missed and which strengthens
the case rather than weakening it.

**THE BLOCKING FINDING: two enumerations still said TWO checks.** `engine.ts`'s `#assertBound`
docstring and `advance-refusal-is-journaled.test.ts`'s FAULTS census each list the vocabulary checks
BY NAME, and neither had gained the third — a census whose whole job is to be total, silently short.
Three named now, FAULTS has a third member, and disabling the check reds 5 of 6.

### §A.84 — dominance, and a docstring that was the defect

The row refused to be "include loop edges in `dagEdges`" and demanded the SET of analyses be named
first. It is, at the site: `topoOrder`, `rule006Cycles`, the fan-out stacks, the critical path,
`ancestors` and `wouldCycle` are RIGHT to drop the back-edge; entry nodes, terminal nodes,
`GRAPH005`, `GRAPH010` and `GRAPH002` were WRONG. `flowEdges` is every edge but `compensation` — the
same predicate `graph/mutate.ts`'s `traversable` uses; `flowAncestors` is a Tarjan condensation read
through `canPrecede`; `GRAPH010` rests on a Cooper–Harvey–Kennedy idom tree over a virtual root
above the entry set. `terminalNodes`, `ancestors`, `dagEdges` and `wouldCycle` are untouched, so
§A.73's coupling survives BY CONSTRUCTION.

**THE BLOCKING FINDING, and it is this wave's sharpest.** The first cut gave `GRAPH010` a
`flowOrder` relation — `flowEdges` with its cycles cut by a depth-first walk, read as *the order the
scheduler realises*. It is not that: the cut only removes an edge whose target is on the CURRENT
path, so a `loop` edge met as a TREE edge stays in and contributes an ordering that is true from
pass two and false on pass one. On `siblings.json` it compiled CLEAN where the base refused, while
the UNCHANGED executor runs the two writers in ONE pass — a real race the compiler had stopped
reporting — and the answer depended on declaration order: 495–527 of 8,000 permutation seeds
flipped, against 0 at base. **The docstring that shipped with it was the defect**: *"the cut can
only withhold a refusal it was already free to withhold"*. It was not free to withhold this one, and
an argument that a guard may only get quieter is not an argument that every refusal it drops was
wrong. It is QUOTED at `computeDominators` rather than deleted.

**A second blocking finding in the same round:** moving `terminalNodes` emptied it on every looping
graph, so `GRAPH002` went dead. Reverted to base; `rule002` now asks *dead iff no output writer in
`flowAncestors(t) ∪ reach(t)`*, which is §A.86.

**What the final review measured**, from a `git archive` extract: a dominance ORACLE over 435,916
pairs across 12,071 graphs (3,071 multi-entry) with **0 mismatches**; permutation flips **0/8,000**;
**no graph that compiled at `be29cb43` is refused at HEAD** over 12,000 random graphs; loop-free
acyclic graphs bit-identical. **And the reviewer WITHDREW its own round-1 claim** that an empty
terminal set broke `loom exam attest` — `examShape` is the same at base and head, a grader being a
leaf. Recording the withdrawal is the point: a reviewer's finding is a claim too, and this one was
re-run rather than believed.

**The cost, recorded at `computeDominators`:** the growth exponent is untouched at n^0.99, and the
500-node compile constant went 21 → 54 → 39 ms, the last after a `writersByChannel` precompute.
`scale.test.ts`'s comment table carried the pre-§A.84 read counts and was corrected in this
settlement: ~101 reads per element is ~120, 548,000 is 649,239, one pass is 22.4× rather than 18.9×.
The assertion — the exponent — never moved.

### The port — a third workflow, whose overlap with the other two stops at six mechanisms

`examples/graphs/grant-access.json` grants temporary access to a production resource and **routes
the decision by how much human it needs**: a public-tier read is automatic, a privileged one goes to
a gate, anything outside policy is refused. 13 nodes over four node types, 14 edges over three
kinds, 5 `function` bodies, 13 request fixtures, and a suite of 26.

**Four mechanisms no other shipped graph holds** — a `router` with two cases and a `fallbackEdge`, a
`kind: "error"` edge, a reducer that is neither `replace` nor `append_ordered`, and a node reached by
two mutually exclusive paths that runs exactly once. It overlaps port 1 in `human_gate`, `function`,
`tool`, `fs.*`, `seq` and `replace`, and port 2 in `conditional` besides, and nowhere else.

**Seven product friction entries, NONE closed**, which is the difference from both earlier ports:
this lane's brief was to record. Five became rows (§A.90–§A.94), one is a docs gap the port itself
filled (`examples/README.md` §10), and one is port 2's F9 canonical-form mechanism in a new and
worse manifestation — a document that ACCUMULATES holds N−1 alphabetised entries and one that is
not, so it looks hand-edited. It is recorded in the log and not as a row, because port 2's F9 has no
row either — **there is no canonical-form row in `TODO.md` to attach it to**, which was checked
rather than assumed (`/usr/bin/grep -an 'canonical' TODO.md` finds five hits and none is a row).
If canonical form is ever to be a row, F9 and F6 are its two measured manifestations.

**F5 is the one to read, and it is §A.90.** An `error` arm is handed no reason: every `fs.read`
failure is one code, so *there is no ledger*, *the ledger is there and I could not read it* and *the
sandbox refuses that path* are the same event. `chmod 222` on the ledger made the undefended
workflow rebuild the document from an empty history and exit 0 — a prior grant destroyed in silence.

**And the defence is the finding.** A reviewer showed the lane's *"not closable from this side"* was
unestablished — a `tool` node is neither a function nor a hook, and `fs.glob` lists a file `fs.read`
cannot open — so the graph gained a `look` node. The NEXT reviewer showed the defence FAILS OPEN in
the identical shape: `fs.glob` answers `(no matches)` for *nothing here* AND for *cannot enumerate*,
so `chmod 333` on the parent directory reproduces the original loss with the defence in place. Four
glob patterns were tried and none yields a signal. **A workaround for a fail-open guard is a guard,
and nobody audited it as one** — written by an author who had just spent a section explaining that
lens about somebody else's code. The hole is pinned NEGATIVELY as a KNOWN HAZARD asserting today's
loss, and the claim is narrowed in four places to the one case the defence covers.

**§4 is nine defects in the port's own workflow, and the largest group is a different class from
port 2's.** Port 2's F14 was eight instances of *the report asserting something the run had not
established*; here FIVE of the nine survived because **nothing distinguished them** — two renewal
guards could be DELETED with the suite green, found by the author's own MUTATION sweep and by a
reviewer's, not by reading. Four of those five were hidden by one fixture that satisfies every guard
at once. **Counted from the table rather than carried:** the table lists nine rows, six of them
blocking, attributed to the mutation sweep (one) and two review rounds (six, then two). §5's prose
says round one found *seven* where its own table lists six — a disagreement left standing in that
doc rather than edited there. A third re-drive then found a tenth the table does not hold: *"each
still losing the ledger in silence"*, written in round two's own correction and contradicted by the
table four lines below it.

**One thing an example shows for the first time:** a failed run's landed `fs.write` is rolled back
by `fs.restore` with NO compensation edge declared — journal-driven, visible in `loom trace` as
`loom.tool (compensate) [ok]`. Worth knowing before anybody adds a compensation edge they do not
need.

---

## 3 · What opened

**Ten rows**, and their provenance is the point. `TODO.md` is the authority; each carries a repro
this settlement RAN on the merged HEAD. Five came out of a stranger driving the shipped binary, five
out of probing what the four closures now compute. A wave whose new rows are all shadows of its old
ones has not been driven.

**The closures' residue (§A.85–§A.89).**

- **§A.85 — a `RunGraph` whose `when`/`until`/router-case `when` is not a string is refused at RUN
  time, mid-`#commit`, and the run is left `running` with NO terminal row.** §A.78's refusal is
  correct and arrives in the wrong place: the first `advance` throws `E_EXPR_INVALID` after work is
  committed, and a second answers `status: running`. PRE-EXISTING and now merely typed — at the base
  the array case OOMed and `null` threw a bare `TypeError`. Closing it is a FOURTH `#assertBound`
  vocabulary check over **edges AND router cases** (`#expr` has three call sites, and an edge-only
  check would be partial, which is why the bounds lane declined to write one), joining `FAULTS`.
- **§A.86 — `rule002`'s `reach(t)` half is reachability the run may never REALISE.** A loop with
  `maxIterations: 1` whose only output writer sits behind a never-true `conditional` after the
  back-edge: base warned `GRAPH002_DEAD_END`, head is silent. It is an over-approximation in the
  QUIET direction at WARNING level, and it is the price §A.84 paid for a rule that was wrong about
  every looping graph. Honest closing needs the loop bound and the edge condition — or an ARGUMENT,
  written at the rule, that a warning may over-approximate. **The second is probably right**; the
  row exists so somebody decides rather than discovers.
- **§A.87 — `GraphMetadata.version` is declared `number` and an in-tree fixture writes `"1.0.0"` and
  runs.** §A.81(a) tagged it `unknown` rather than refuse working code, and said so in place. One
  edit to the type, one to `test/cli/guards-lane-extension-engine-seams.test.ts:90`, one tag — and a
  product decision nobody has taken.
- **§A.88 — `contextProjection.take: null` compiles clean**, because `checkProjectionValues` reads
  `null` as ABSENT where it refuses `"banana"`, `["x"]`, `{a:1}` and `true`. Pinned POSITIVELY as a
  one-member `STILL_ACCEPTED` set, so it cannot stop existing in silence and the pin goes red when
  it closes.
- **§A.89 — one dominance relation, two implementations**, in `graph/validate.ts` (an idom tree, on
  every compile) and `graph/mutate.ts` (V×V bitsets, on a proposed mutation). The costs are right
  where they are; the duplicate is NAMED at the site, which is the only reason this is a row and not
  a defect. Unifying needs `mutate.ts`, which the lane did not own.

**The port's five (§A.90–§A.94), each met by a stranger and not by a reviewer.**

- **§A.90 — an `error` arm is handed no reason, so a workflow rebuilds its own record from an empty
  history and exits 0. SILENT DATA LOSS, and the priority row of this settlement.** Three outcomes
  are one code and the `content` that distinguishes them reaches no channel. Closing it is a
  PROJECTION of the failure into a channel an arm may declare in `reads` — a new channel SHAPE, the
  same decision §A.83 is waiting on, one door over. A structural alternative is on the owed list: a
  per-run ledger file cannot lose an entry to a failed read, and converts a silent loss into a
  spurious gate.
- **§A.91 — a by-hash graph lookup COMPILES every graph in `graphs/` and prints the others'
  diagnostics on stderr.** Three verbs and not four, measured one at a time: `replay` and `trace`
  leak, `approve` leaks, `audit` does NOT — it never looks the graph up. `--graph` is both the
  workaround and the confirmation. An operator approving a production change is shown warnings
  naming a file they did not run.
- **§A.92 — a gate's approvers are STATIC, so a graph cannot ask the people its own data names.**
  The shipped gate shows `owners: ["u:ravi","u:mina"]`, read from policy at run time, and accepts
  only `u:you`. This is the interesting half of "no k-of-n field": k-of-n lives in `join`, which
  answers *how many* and cannot answer *which people*, because the N gates are static too.
- **§A.93 — a denial and a failure-to-decide wear one code.** `E_FUNCTION_REFUSED` for both, and
  only the node name inside the message separates them, so a wrapper must scrape prose. The graph
  draws the line on purpose — refusing only when it cannot DECIDE, routing everything it decided
  against to a `deny` node — and cannot publish it.
- **§A.94 — `GRAPH010` calls the `seq` target and the `error` target of ONE node concurrent
  writers.** **NOT §A.84**: `error` is not on that exclusion list, so the edge IS visible and its
  EXCLUSIVITY is not. The closest relative is §A.40, which is CLOSED — the same shape one edge kind
  over — and §A.48 records what that exemption does not cover; this would be a second and NARROWER
  one, because a `seq`/`error` pair's exclusivity is structural rather than dataflow-dependent.

**Recorded WITHOUT opening a row, which is a third category.**

- **§A.62's row gained a note**: "closes at TWO" is now "closes at two, TWICE". `readableLoopBound`
  and `readableFanoutWidth` are the same predicate under two names, deliberately not folded because
  each refusal must say what ITS reader does with the value — and **the only thing keeping them in
  step is a paragraph**, which says so. A drift risk, not a defect: nothing to fix until they
  disagree.
- **§A.76's row gained a note**, because §A.81(b) gave its residue a cost it did not visibly have:
  `attach` on a run THIS PROCESS ALREADY HOLDS silently discards the supplied graph, so NEITHER
  `#assertBound` vocabulary check ever sees it. Measured with a control that differs in one step —
  the same bent bound is REFUSED after a restart. Closing it is the owed *should `attach` be honest*
  decision and not a separate defect.
- **§A.80's struck row records** that a throwing ACCESSOR on an edge object still throws at the
  property read, base extent, now that the renderers are total over values.

**Nearest neighbours left untouched** — named rather than totalled: §A.77 (still open; no lane took
it), §A.82 and §A.83 (both waiting on the same kind of decision as §A.90), §A.66, §A.71, §D.8,
§A.60, §A.48. **Of the ten new rows, §A.90 is the one a maintainer should look at first**: it is the
only row in `TODO.md` that ends in destroyed data with exit code 0.

---

## 4 · Decisions taken without the user this wave

Seven taken, and then the list of what was deliberately NOT taken, which is longer and is the more
important half.

1. **§A.78's guard is a type test AND a progress assertion, in `lex` rather than in `checkExpr`.**
   The row asked for progress; the type test is the one that produces a diagnostic an author can
   read. `lex` is where both go, because `parseExpr` is the executor's own entrance — the same
   argument §A.62 closed at two on.
2. **§A.81(a) leaves three fields deliberately untagged**, each with its reason at the site:
   `metadata.version` (`unknown`, because tagging it `number` refuses an in-tree graph that runs —
   §A.87), `channel.initial` (`unknown`), and `policy.expansion`'s bounds (`isPositiveInt` kept,
   because a `count` tag refuses LESS). A guard that cries wolf on correct code is worse than no
   guard, and the alternative was to tighten working graphs to make a table look complete.
3. **§A.84's `terminalNodes` was REVERTED to base rather than made loop-aware**, and `rule002` asks
   a different question instead. The first cut's version emptied the terminal set on every looping
   graph; the revert keeps every rule that reads it unchanged and confines the fix to the one rule
   that was wrong.
4. **`GRAPH010` rests on DOMINANCE, not on a traversal order.** Dominance is a property of the
   graph, so no permutation can change it, and an ordering it reports holds on every pass. The
   rejected alternative shipped first and is quoted at the site.
5. **§A.80's refusal stays NON-FATAL and its position changes.** Byte-identical text, a different
   place in the list, and a refusal that can be lost behind an earlier fatal — all three disclosed
   at the site rather than discovered by the next reader.
6. **The port ships the F5 defence with a hole, pinned NEGATIVELY**, rather than deleting the
   defence or claiming it closes F5. It covers exactly one of four ways the read can fail; the
   KNOWN HAZARD test asserts today's LOSS, and when §A.90 closes that test should FAIL and be
   deleted along with the `look` node rather than loosened.
7. **The port's ledger file is declared a TRUST BOUNDARY** in the doc and in `policy.json`'s own
   note: anything that can write `out/` can forge `decidedByKind: "human"` and buy an auto-renewal.
   Stated rather than defended, because defending it is the per-run-ledger redesign on the owed
   list.

**And these were NOT taken. Every one is owed to the maintainer, and four are owed for the FIFTH
wave running.**

- **§A.68 — `skip` vs `fail` on the shipped example.** Unchanged and unanswered. **This is still the
  decision the settlement most wants answered.**
- **§A.77's compile-time refusal** — whether `validate.ts` should refuse `k > branches.length` for a
  barrier whose every member is static and unfanned. **No lane took it this wave.**
- **§A.82's per-key classification design** and **§A.83's `fs.read` truncation channel shape** —
  both are *what does a channel carry*, and **§A.90 is now a third instance of the same question**,
  which is the argument for answering the shape once rather than three times.
- **Whether `attach` should be honest** (§A.76's residue), now with a measured cost: it is the
  reason neither `#assertBound` check can see a graph attached to a held run.
- **`EDGE_FIELDS`' shape break, and NOW ALSO `POLICY_FIELDS`/`NESTED_FIELDS`**: an exported name
  whose TYPE changes from an array to a record passes `check-surface.mjs` as "unchanged". The new
  half is worse in one direction — an out-of-tree `.includes` or spread reader THROWS, while a
  `.length` or indexed-loop reader fails SILENTLY, validating nothing. Whether that guard should pin
  shape, and whether such a change may land under `fix:`.
- **§A.71** (does a budget/fatal floor cascade to a subgraph child), **§D.8** (must a join's inbound
  edge be `kind: join`), **§A.70** and **§A.72** — all four still owed, all four carried into a
  fifth wave.
- **`metadata.version`: a number or a semver STRING** (§A.87).
- **Whether `GRAPH002`'s reachability should be *realisable* rather than *reachable*** (§A.86), or
  whether a warning may over-approximate and say so.
- **The port's structural alternative to F5**: a per-run ledger file cannot lose an entry to a
  failed read. It converts a silent loss into a spurious gate, which is the direction a guard is
  allowed to fail in.

---

## 5 · Facts you would otherwise rediscover

- **The rate-limit kill did NOT recur this wave.** All three lanes and every review ran to
  completion; each review was dispatched synchronously by the orchestrator from a `git archive`
  extract and reported back. The previous handoff's recovery procedure — resume by MESSAGE rather
  than re-dispatch, reports to a durable place first, gate once per wave — exists for when it does,
  and is left in place unused.
- **All three worktrees were handed out at `086e47d2`**, which is not merely the wrong branch: it is
  the EAgent-era `init` commit, not a `loom` commit at all. Each lane detected it and self-reset to
  `be29cb43` before its first commit. **This is the FOURTH wave running.** Check `git worktree list`
  immediately after dispatch; the orchestrator's 20-second check saw `be29cb43` because the lane had
  already reset.
- **There is no `timeout` binary on this box** (`which timeout gtimeout` → not found). Lanes bounded
  long commands with `perl -e 'alarm N; exec @ARGV'`.
- **A mutation pin that runs IN-PROCESS can take the test runner's whole heap** — seven tests report
  and the file is then charged as ONE unnamed failure, and the `finally` that cleans up never runs,
  so temp directories leak. Run mutation pins in a CHILD process with a heap cap and capture the
  exit as text. Both of §A.78's pins do.
- **"Pin the property, never the operator."** A regex pin asserting the source reads
  `i === lastStart` would have forbidden the strengthening to `i <= lastStart` that a reviewer's
  injected backwards branch proved necessary.
- **A docstring arguing that a guard's unsound case is SAFE was the defect** — *"the cut can only
  withhold a refusal it was already free to withhold"*. A permutation fuzz refuted it in one run.
  When a change makes a guard quieter, the question is not whether it MAY be quieter; it is which
  refusals it dropped.
- **"Coverage of the guard is not coverage of the gap" recurred, in a new shape.** A renewal-window
  test set the window to 0 and exited through a guard clause BEFORE the comparison it claimed to
  test — green, and testing nothing. Mutation found it; reading did not. Deleting either of two
  renewal guards kept the port's suite green.
- **Reviewers must not share the builder's worktree** — held again: every review this wave ran from
  a `git archive` extract, and every blocking finding came from one.
- **A lane's worktree has no `node_modules` of its own**, so three root-level harness tests (which
  spawn `typescript` from a temp directory) are RED there and green in an extract with a real
  install. Do not read those three as a lane regression. *(Reported by the lanes and by the
  orchestrator's verification; not re-measured in this settlement, because the worktrees are gone.)*
- **A reviewer's own claim was WITHDRAWN on re-run**, and the withdrawal is recorded rather than
  quietly dropped: the validate lane's reviewer claimed an empty terminal set broke
  `loom exam attest`, then measured `examShape` at base and head and found them the same. A
  reviewer's finding is a claim, and the same rule applies to it — reproduce by running.
- **Cross-lane facts come from the orchestrator.** This wave's instance was useful rather than
  costly: the port lane was told, mid-flight, that §A.84 would take `harden-config` to zero
  warnings, so its F2 repro was rebuilt around a graph it drops in itself and the count survives the
  closure. A repro that counts another lane's diagnostics measures the other lane.
- **`node --test packages/core/test/<dir>/` still does not work in this tree** — it needs the glob,
  `node --test "packages/core/test/<dir>/**/*.test.ts"`. The bare directory form reports
  `pass 0 / fail 1`.
- **`examples/.loom` and `examples/out` are created by driving the examples** and are gitignored.
  This settlement deleted both after its repros, as the previous one did; the port's §2 opens by
  telling a stranger to `rm -rf out .loom`, so a stale journal there makes the first assertion
  unreproducible. Two pieces of root litter remain and are still left alone: empty `graphs/` and
  `resources/` (27 Aug).

---

## 6 · Assessment and direction

**This section is the wave orchestrator's assessment, recorded here on 2026-09-22 at the
maintainer's request. It is NOT a lane measurement** — §§1–5 above report what three lanes built and
what the settlement re-ran; this one is a judgement about where the project stands, written into the
files so the next reader finds the direction here rather than in a chat. Every number in it was
re-measured against `29c8b9ec` before it was written, and each claim names where to check it.

### Standing

**The Sequence is fully accounted for, and the set is NAMED because the first draft of this
assessment mis-stated it.** It said items 1–28 were "closed, half-closed or reclassified"; **that
is FALSE, and `DESIGN.md`'s own tables say so** — item **18** (G.5(a): after a MUTATION the
successor carries no recorded manifest, so a gate decision on it never checks the resources behind
its refs) and item **24** (A.29) are marked **open**. The set, re-derived from the three tables at
`29c8b9ec`: **two open (18, 24), two half (8's version pin, 13's second half), one reclassified
(22), and twenty-three done.** No item had been opened since 2026-09-02, when 15–28 were.

**Against that, all measured at `29c8b9ec`:** `git rev-list --count HEAD` → **1,170** commits, over
**49** days (first commit 2026-08-04, last 2026-09-22); `npm test` → **4,117 pass / 0 fail**, 0
cancelled / skipped / todo; **three workflows ported with zero changes under `packages/core/src`**
(`CLAUDE.md` "The goal"); and a guard seen refusing a real commit rather than merely existing —
`check-surface.mjs` refused `added: BlockFieldType` in this very wave (`TODO.md` §State). Each of
those numbers moves with the next commit; the command beside it is the durable half.

**The three properties, each with its real bound.** Property 1 holds mechanically, with a blind spot
the last two waves both hit: capability landing under `fix:`, now twice as a SHAPE change to an
exported constant — `EDGE_FIELDS` last wave, `POLICY_FIELDS`/`NESTED_FIELDS` this one — which
`check-surface.mjs` reads as "unchanged" (`surface guard ok: 542 public exports, unchanged`) and
`check-kernel.mjs` does not ask a trailer for, `fix` being free to touch the kernel. Property 2
holds against a named set, re-counted for this section from `README.md`: **18 and 3**, seventeen
things that need no fork and three that do. Property 3 holds **only under the five assumptions in
`CLAUDE.md` §3**, and one of them — §A.29, Sequence item **24** — is an open defect with three
refused mechanisms. **Item 18 is its neighbour and is easy to miss**: G.5(a) leaves a mutated run's
successor with no recorded manifest for `#assertBound` to compare, on `graph/mutate.ts`, the verb
self-improvement would have to use. Those two are the only open items in 1–28.

**The open-row count rose, and that is the discipline working.** **46 → 51 → 56**, counted with
`TODO.md`'s own census command at the three settlement commits `bde693e2`, `0a9483c0` and
`279b5c73`. The rise is strangers driving the shipped binary (five of the ten new rows are port 3's
friction log) and probing what the closures now compute (the other five) — not decay. §3 above
makes the same point from the other end.

**The binding constraint is DECISIONS, not code.** Four are owed for a fifth wave running (§A.71,
§D.8, §A.70, §A.72 — §4 above), **§A.82, §A.83 and §A.90 are ONE unanswered question** (what a
channel carries when a tool fails, truncates or holds a secret, now `TODO.md` §D.10), and the
decision this settlement most wants answered is **one word in a shipped example** (§A.68, `skip` vs
`fail` in `examples/graphs/two-person-approval.json` — a **STRUCK** row, so look in its 2026-09-22
addendum rather than for an open one; the graph's behaviour is documented and correct, and the
question is which mode the example should TEACH). None of these is blocked on anything being
built.

**And the goal's first verb has never been done.** `CLAUDE.md` opens *"Install it, describe what
they want done…"*. `@loom/core` is `private: true`; `npx --yes @loom/core --version` is a 404 and
`npm publish --dry-run` exits **0** having skipped the package. Distribution sat under
"Deliberately not sequenced" carrying TWO conditions, and they are retired differently: the
superseded one ("revisit when item 4 lands") is the one `DESIGN.md` itself called wrong, while the
STANDING one — *"somebody who is not the maintainer asking to run this"* — was never called wrong
and is **abandoned rather than met**, because a stranger cannot ask to run a thing they cannot
obtain. `TODO.md` §H.15 is the row; `README.md`:55 and :69–72 already document both halves.

**§C gates the UI and is blocked on journal vocabulary, not on a view.** Re-measured: six designed
span names have no producer (§C.1, `/usr/bin/grep -anc 'name: "loom\.' …/telemetry/spans.ts` → 7,
which undercounts by two because `loom.model`/`loom.tool` are one ternary) and eight of eleven
documented span attributes are not derivable (§C.2, the three that are count **3**). Every one of
the eight needs a `journal/events.ts` change, and therefore a `Kernel-seam:` trailer.

### Direction, in priority order

1. **One maintainer's SITTING answering the owed set** — and the channel shape ONCE for §A.82,
   §A.83 and §A.90, **§A.90 first**: it is the only row in `TODO.md` that ends in destroyed data
   with exit code 0. Then §A.68, §A.77, the exported-constant shape-break policy (`TODO.md` §D.11),
   `attach` honesty (§A.76's residue), §A.71, §D.8, §A.70, §A.72, `metadata.version` (§A.87),
   `GRAPH002`'s realisable reachability (§A.86), and the per-run-ledger alternative to port 3's F5.
   §A.68 and §A.76 are STRUCK rows whose decisions live in an addendum and a note; the rest are
   open. The same list is enumerated in `DESIGN.md` after item 31, under the paragraph saying why a
   decision is not a Sequence item at all.
2. **Sequence DISTRIBUTION now** — a published binary and a stranger-facing install. Item 29.
3. **Close §A.29 or re-scope property 3 — item 24, which keeps its number rather than being
   reopened as a new one**, with a dated amendment in its own cell. The money decision behind it is
   SMALLER than when it was deferred: "a second input shape for the self-improvement corpus" was
   waiting on a second port, and ports 2 and 3 have happened. **Item 18 belongs beside it** — the
   mutation half of the same property, and neither is worth doing alone.
4. **The journal-vocabulary seam for §C** — two or three events, each its own `Kernel-seam:`
   trailer. Item 31, and it covers §C only: A.11 and E.2 are the same kind of blocker with no
   failing command and stay in "What is left over".
5. **A FOURTH workflow port only after (1) and (2).** It is worth as much as the first three — that
   claim is unchanged — but friction met by a stranger who could not install this is friction
   nobody logs.

### The one-sentence version, and what it qualifies

**A runtime whose mechanism is finished and whose product has not been started; the next unit of
work worth most is a maintainer's sitting of decisions followed by an install path, not a ninth
wave of invariants.** `CLAUDE.md` says *"the next real workflow somebody ports is worth more than
the next invariant somebody proves"* — that ranking is unchanged and still holds BETWEEN those two.
What this section adds is that a decision and an install path now outrank both, and for the same
reason the port argument was made in the first place: an invariant nobody exercises names no seam,
and a runtime nobody can install has no strangers to exercise it.

---

## Repros

Every command below was re-run on the merged settlement HEAD for this document. The lane probes live
in `.agent/wave-2026-09-22b/probes/` (gitignored), copied from the worktrees and the reviewers'
extracts and re-pointed at the main checkout; `eng.mjs` takes the repo directory as `argv[2]`.

### §A.78 — the OOM is a diagnostic now

```bash
node --max-old-space-size=400 .agent/wave-2026-09-22b/probes/probe-when-null-oom.ts
```
```
compiling with when: [null] …
returned: GRAPH004_EXPR
```

The base, for comparison, printed
`FATAL ERROR: Ineffective mark-compacts near heap limit`. The deliberate tightening beside it:

```bash
node <checkExpr over four sources> "$PWD"      # and again against a be29cb43 extract
```
```
                 HEAD                                   be29cb43
plain string     must evaluate to a boolean, got number  (same)
new String       must be a string, got an object         must evaluate to a boolean, got number
["a"]            must be a string, got an array          Cannot convert undefined or null to object
["1"]            must be a string, got an array          must evaluate to a boolean, got number
```

### §A.79 / §A.80 — a crash and a hole, both now diagnostics

```bash
node .agent/wave-2026-09-22b/probes/probe-subgraph-no-inputs.ts
node .agent/wave-2026-09-22b/probes/probe-subgraph-kind.ts
```
```
returned: GRAPH003_MALFORMED

compile(parent), child edge has kind: 42 AND maxWidth: "24"
  ok: false  errors: GRAPH003_UNKNOWN_EDGE_KIND, GRAPH007_BAD_MAX_WIDTH

validateGraph ALONE, kind: 42 on a top-level edge (no compile pre-pass)
  errors: GRAPH003_UNKNOWN_EDGE_KIND
```

### §A.81 — the loop bound the executor now reads

```bash
node .agent/wave-2026-09-22b/probes/probe-maxiter-executor.mjs
```
```
maxIterations=6     advance=status=succeeded n=["s","x","x","x","x","x","x"]
maxIterations="6"   advance=REFUSED E_GRAPH_INVALID: … has a loop edge whose maxIterations this
                    build cannot read: "e2" (maxIterations "6") …
maxIterations={}    advance=REFUSED E_GRAPH_INVALID: … (maxIterations an object) …
```

The third line was `status=succeeded n=["s","x"]` at the base: one pass of six, the conditional exit
never reached, and the run reporting success.

### §A.84 — the canonical loop shape, and what the executor does with it

```bash
node .agent/wave-2026-09-22b/probes/probe-loop-invisible.mjs
```
```
compile: ok

same graph, merge_object (the diagnostic's own first fix:) -> compile: ok
  run status=succeeded  node order=["parse","audit","fix","audit","fix","audit","fix","audit"]
  "fix" ran at t=0 beside "parse": false
```

At the base this printed `GRAPH005_UNPRODUCED_READ` and `GRAPH010_CONCURRENT_WRITE`, and taking the
diagnostic's own first `fix:` ran the fixer before the first audit. And on the shipped graphs:

```bash
cd examples && for g in graphs/*.json; do printf '%-34s ' "$g"; loom compile "$g" 2>&1 | /usr/bin/grep -acE '^(!|✗)'; done
```
```
graphs/fan-out-join.json           0
graphs/grant-access.json           0
graphs/guarded-write.json          0
graphs/harden-config.json          0      ← 3 at be29cb43, all of them §A.84
graphs/review-bench.json           0
graphs/self-review.json            0
graphs/triage-failures.json        0
graphs/two-person-approval.json    0
```

### §A.85 — a refusal in the wrong place

```bash
node .agent/wave-2026-09-22b/probes/a85-expr-runtime.mjs "$PWD"
```
```
conditional when: [null]
  1st advance: THREW E_EXPR_INVALID: expression must be a string, got an array
  2nd advance: status=running
  terminal rows: run.failed=0 run.succeeded=0 | last row: effect.completed
loop until: [null]
  1st advance: THREW E_EXPR_INVALID: expression must be a string, got an array
  2nd advance: status=running
  terminal rows: run.failed=0 run.succeeded=0 | last row: effect.completed
```

### §A.86 — the same file, two compilers

```bash
node .agent/wave-2026-09-22b/probes/compile-file.mjs "$PWD" \
  .agent/wave-2026-09-22b/probes/R1_unreachable_pass_writer.json
node .agent/wave-2026-09-22b/probes/compile-file.mjs <be29cb43 extract> \
  .agent/wave-2026-09-22b/probes/R1_unreachable_pass_writer.json
```
```
ok: true
  (no diagnostics)

ok: true
  warning GRAPH002_DEAD_END: terminal node "t" ends a path on which no declared output is ever written
```

### §A.76's note — `attach` on a held run, with its control

```bash
node .agent/wave-2026-09-22b/probes/attach-discards.mjs "$PWD"
```
```
attach on a run this process HOLDS  -> NO REFUSAL status=succeeded n=["s","x","x","x","x","x","x"]
attach after a RESTART (not held)   -> REFUSED E_GRAPH_INVALID
```

The first line ran the GOOD bound — seven items, six iterations — so the bent graph reached nothing
at all.

### §A.90 — the data loss, on the shipped graph with its defence in place

```bash
cd examples && rm -rf out .loom
loom run graphs/grant-access.json --input '{"requestPath":"access/requests/docs-site-read.json"}' >/dev/null 2>&1
jq -c '[.grants[].who]' out/access-ledger.json
chmod 222 out/access-ledger.json && chmod 333 out
loom run graphs/grant-access.json --input '{"requestPath":"access/requests/docs-site-read-ravi.json"}' 2>/dev/null \
  | jq -c '{status, historySource: .outputs.decision.historySource}'
chmod 755 out; chmod 644 out/access-ledger.json; jq -c '[.grants[].who]' out/access-ledger.json
```
```
["u:sam"]
{"status":"succeeded","historySource":"none"}
["u:ravi"]
```

And the case the defence DOES cover, which is the two-directions half — the same `chmod 222` with
`out/` still listable:

```
{"status":"failed","historySource":null}
["u:sam"]                                  ← the grant survives
```

### §A.91 / §A.92 / §A.93 / §A.94 — the other four product rows

```bash
cd examples && loom compile graphs/zz-noisy.json 2>&1 | head -1
loom replay "$RUN" 2>&1 >/dev/null | /usr/bin/grep -aE '^! ' | /usr/bin/grep -av 'grant-access' | sed 's/:.*//' | sort | uniq -c
for v in audit replay trace; do printf '%-8s ' "$v"; loom $v "$RUN" 2>&1 >/dev/null | /usr/bin/grep -a -c 'zz-noisy'; done
loom replay "$RUN" --graph graphs/grant-access.json 2>&1 >/dev/null | /usr/bin/grep -a -c 'zz-noisy'
```
```
! zz-noisy.json: GRAPH005_UNPRODUCED_READ: node "n" reads "a", which no upstream node writes and which is not a graph input
   1 ! zz-noisy.json
audit    0
replay   1
trace    1
0
```

```bash
loom gates "$RUN" | jq -c '.[0] | {approvers, owners: .reads.decision.owners}'
loom approve "$RUN" "$GATE" --as u:ravi
```
```
{"approvers":["u:you"],"owners":["u:ravi","u:mina"]}
E_GATE_NOT_AUTHORIZED: gate "gate_01M348BRFEVZ0F94RW25SG00D3" does not name "u:ravi" as an approver
```

```bash
loom run graphs/grant-access.json --input '{"requestPath":"access/requests/payments-kms-admin.json"}' 2>/dev/null | jq -c '{status, class: .error.class, code: .error.code}'
loom run graphs/grant-access.json --input '{"requestPath":"access/requests/not-a-request.txt"}'      2>/dev/null | jq -c '{status, class: .error.class, code: .error.code}'
```
```
{"status":"failed","class":"validation","code":"E_FUNCTION_REFUSED"}   ← policy says no
{"status":"failed","class":"validation","code":"E_FUNCTION_REFUSED"}   ← could not read the request
```

```bash
sed -i.bak 's/"history": { "type": "object", "reduce": "merge_object", "onConflict": "last_by_branch" }/"history": { "type": "object", "reduce": "replace" }/' graphs/grant-access.json
loom compile graphs/grant-access.json; mv graphs/grant-access.json.bak graphs/grant-access.json
```
```
✗ grant-access.json: GRAPH010_CONCURRENT_WRITE: nodes "prior" and "first-grant" can run concurrently
  and both write "history", whose reducer `replace` is not multi-writer safe
   fix: change channel "history" to a multi-writer-safe reducer, or sequence "prior" and "first-grant"
E_GRAPH_INVALID: graph has 1 error(s): GRAPH010_CONCURRENT_WRITE
```

### The port, driven on the shipped binary — three endings

```bash
npm run build:binary && export PATH="$PWD/bin:$PATH" && cd examples && rm -rf out .loom
loom compile graphs/grant-access.json                                                   # → ok, no diagnostics
RUN=$(loom run graphs/grant-access.json --input '{"requestPath":"access/requests/orders-db-backfill.json"}' 2>/dev/null | jq -r .runId)
ls out                                                                                  # → No such file or directory
GATE=$(loom gates "$RUN" 2>/dev/null | jq -r '.[0].gateId')
loom approve "$RUN" "$GATE" --as u:you && ls out
loom replay "$RUN" | jq -c '{match, hermetic}'
loom run graphs/grant-access.json --input '{"requestPath":"access/requests/docs-site-read.json"}'     2>/dev/null | jq -c '{status, ceremony: .outputs.decision.ceremony}'
loom run graphs/grant-access.json --input '{"requestPath":"access/requests/orders-db-too-long.json"}' 2>/dev/null | jq -c '{status, code: .error.code}'
```
```
ls: out: No such file or directory
access-ledger.json
grant.json
{"match":true,"hermetic":true}
{"status":"succeeded","ceremony":"auto"}          ← no gate at all, exit 0
{"status":"failed","code":"E_FUNCTION_REFUSED"}   ← exit 1
```

No `--extension-module`, no fork, and `git diff --name-only be29cb43..9ee826a5 -- packages/core/src`
is empty. The gate must be answered by `u:you`, which the graph names.

### The census cells

```bash
npm run build:binary && npm run check          # → exit 0; tests 4117 pass / 0 fail
node scripts/check-surface.mjs                 # → 542 public exports, unchanged
node scripts/check-kernel.mjs                  # → 10 files pinned, 879 commits judged, 16 declared seams
node scripts/check-zero-dep.mjs                # → ok, 67 files scanned
node -e 'EVENT_TYPES.length'                   # → 51
node -e 'Object.keys(CODES).length'            # → 60
sed -n '/^## Extending it, and where that stops$/,/^## Why this exists$/p' README.md | /usr/bin/grep -a -c '^| '      # → 18
sed -n '/^## Extending it, and where that stops$/,/^## Why this exists$/p' README.md | /usr/bin/grep -a -c '^- \*\*'  # → 3
/usr/bin/grep -aoE '^- (~~)?\*\*[A-Z0-9]+\.[0-9]+ ' TODO.md | wc -l     # → 156
/usr/bin/grep -aoE '^- ~~\*\*[A-Z0-9]+\.[0-9]+ '    TODO.md | wc -l     # → 100
/usr/bin/grep -acE '^[0-9]+\. \*\*' TODO.md                             # → 19  (§F, counted its own way)
```

The NUL/UTF-8 census is not a one-liner and `grep` cannot do it — `grep` reports a skipped file only
when it also MATCHES your pattern, so empty output is not evidence. Read every `git ls-files` path,
test `Buffer.includes(0)`, and decode with a fatal `TextDecoder`:

```
tracked 554
NUL 5 [ packages/core/src/evolution/trajectory.ts,
        packages/core/src/journal/payloads.ts,
        packages/core/test/builtin/fs-search.test.ts,
        packages/core/test/run/delivery.test.ts,
        packages/core/test/server/http.test.ts ]
invalid UTF-8 0 []
```

`docs/workflow-port-2026-09-22b.md` quotes control characters in prose and wrote none, which is the
whole reason this was re-run: `ea929446`, two waves ago, is a commit whose entire subject is a
literal NUL the editor produced from an escape in a comment.
