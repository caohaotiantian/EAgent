# Handoff — 2026-09-24

The current handoff. It supersedes `docs/handoff-2026-09-23.md`, which is left in place because
other documents cite it; everything in it this wave did not move is still true — **with five
exceptions, named here rather than edited there.** (1) Its §7 table's Q12 names `rule002DeadEnds`;
the function is `rule002Terminals`. (2) Its §5 flag 2 and §3 say `fs.restore` cannot undo a create;
it can since `b7c1ccc6` (Q3 = (a)). (3) Its §7 Lane B brief asked to re-measure *"mutation is
unreachable from the shipped binary"* — it was re-measured and was FALSE (§2 below). (4) Its §7
"Order" puts §A.99 in wave 2; it landed in wave 1, inside Lane A, once Q3 was answered. (5) Of its
§7 table, Q1, Q2, Q3, Q11 and Q12 are answered (§4); Q4–Q10 and Q13–Q20 are not.

This one covers **the five `wave-0923-*` lanes from `2af9716a` to `7f576a47`** — that handoff's §7
plan, Wave 1, merged `--no-ff` in its decided order D → B → A → C → E:

| lane | tip | merge | what it is |
|---|---|---|---|
| wave-0923-D, install surface | `6b1a47eb` | `9b8377e9` | §H.17, §H.19, §A.91, §H.16 (Q1), §H.18 (Q2) |
| wave-0923-B, successor binding | `5c236f55` | `c36bbae7` | Sequence item 18 / §G.5, §A.85 |
| wave-0923-A, projection producers | `e2e783ca` | `0420f95b` | §A.83, §A.96, §A.97, §A.99 (Q3 = a); contains B via `f0280957` |
| wave-0923-C-pins, compiler | `3731883a` | `013e7d5e` | §A.95, §A.94, §A.98, §A.88, §A.77 (Q11), §A.86 (Q12) |
| wave-0923-E, item 24's repro | `0558c2a5` | `7f576a47` | §A.29 / Sequence item 24's first task |

Zero conflicts, the TENTH wave running: `git merge-tree --write-tree <merge>^1 <merge>^2` exits 0 for
all five, and its tree equals each merge's own `^{tree}` — no evil merge (re-run for this
settlement). 63 commits; `git rev-list --count` 1,207 → 1,270.

**The wave in one sentence:** property 3's mutation half closed and its promotion half got a
command that fails, D8's truncation producer and rollback landed, the compiler got quieter by
measured enumeration, the install surface became publish-ready — and a compiler lane, proving its
own rule, found a KERNEL defect that every other rule had been assuming away.

---

## 1 · The state

Re-run on `7f576a47` in a worktree at that commit. `TODO.md`'s "State" table carries the same
numbers and is the authority.

| fact | value | command |
|---|---|---|
| the gate | **exit 0** | `npm run check` |
| tests | **4,293 pass / 0 fail** (suites, cancelled, skipped, todo all 0; 4,162 → 4,293) | `npm test` |
| this wave's suites | `run/error-projection` **34**, `builtin/tools` **60**, `run/agent-tool-truncation` **3**, `run/graph-binding` **11**, `run/advance-refusal-is-journaled` **6**, `run/expression-vocabulary` **4**, `graph/error-projection-multiplicity` **16**, `graph/outcome-exclusive-writers` **8**, `graph/quorum-k-above-static-branches` **5**, `graph/subgraph-parent-mapping` **5**, `graph/block-field-types` **7**, `graph/two-person-approval` **13**, `examples-grant` **35**, `examples-harden` **24**, `cli/resolve-recorded-graph` **6**, `cli/version` **10**, `scripts-pack` **3**, `telemetry/otlp` **17**, `evolution/a29-verifier-certified-refused` **2** — all pass | `node --test packages/core/test/<file>` |
| pinned exports | **544**, unchanged | `node scripts/check-surface.mjs` |
| kernel | 10 files, **19 declared seams** (17 → 19: `26a4f358` `feat:`, `4fe87a88` `fix:` with a voluntary trailer); **981** commits judged. Kernel files the wave touched: `graph/spec.ts`, `journal/events.ts`, `run/engine.ts`, `run/projection.ts` | `node scripts/check-kernel.mjs` |
| seams git's parser rejects | **6** of 19 (5 → 6: `4fe87a88`'s trailer continues flush-left) | `git log -1 --format=%B <sha> \| git interpret-trailers --parse`, per seam |
| zero runtime deps | ok, **69 files**, unchanged | `node scripts/check-zero-dep.mjs` |
| error vocabulary | **62** codes, unmoved | `Object.keys(CODES).length` |
| journal vocabulary | **51** event types, unmoved (`graph.mutated` gained a FIELD) | `EVENT_TYPES.length` |
| NUL census | **5** NUL files, **0** invalid UTF-8, of **579** tracked | read every `git ls-files` path |
| README ledger | **18** and **3**, unmoved | the two commands in `CLAUDE.md` §2 |
| README test floor | 3,500+; the probe counts **4,030** `test(` over **378** files | `readme-gaps.test.ts`'s probe |
| open rows | **67** at `7f576a47` (170 rows, 103 struck); **82** after this settlement (203 rows, 121 struck — §A.124 withdrawn in its review) | `TODO.md`'s row-census commands |
| the tarball | `141 files, 1815 KB`, **138** `sourceMappingURL` pointers stripped, 0 in the tarball; `smoke ok (tarball)` — packed from this settlement's `6ef3aca5`, whose `packages/` and `scripts/` equal `7f576a47`'s (`git diff --name-only` → 0) | `node scripts/pack.mjs --out <dir>`, `node scripts/smoke-install.mjs <tgz>`, `tar -xzf <tgz> -O \| /usr/bin/grep -ac sourceMappingURL` → 0 |

`CLAUDE.md` 270 → 270 lines (`wc -l`): the port-2 and port-3 sentences now say which rows closed,
the seam sentence says six of nineteen, the install sentence says the publish is unblocked, and the
START pointer is this file. Nothing was added that a cut did not pay for.

---

## 2 · What closed

**Sequence item 18 / `TODO.md` §G.5 — `4fe87a88` (Lane B).** `graph.mutated` records the
successor's `resolutionManifest`; `#graphIdentityMismatch` compares a held successor against it,
`#rehydrateGraph` checks every replayed one, and both gate doors rehydrate before `gate.decided`.
The row said mutation was unreachable from the binary. It was not: `loom run --grant graph:mutate
--extension-module <an adapter proposing a mutation>` reaches it, and at `2af9716a` an edited
mutation-added `function` ran on `loom approve`, exit 0 — now exit 1, `E_GRAPH_MISMATCH`. An old
journal fails closed; `cancel` is its exit. **§A.85** (`6152c2d8`) rode with it: `#assertBound`'s
fourth vocabulary check.

**D8's second half — §A.83, §A.96, §A.97, §A.99 (Lane A).** The truncation marker is out of the
content at `fs.read`, `proc.exec` and `net.fetch`, `truncated`/`bytes` are served on the projection,
`maxBytes` counts bytes, and an agent is told in-band by `modelToolContent` (`26a4f358`, the wave's
one kernel `feat:`). A task whose own call was undone projects no value (§A.96). `fs.read` refuses
a FIFO, device or socket before it blocks (§A.97). `fs.restore` undoes a CREATE by recorded identity,
refusing on changed bytes (§A.99, Q3 = (a)) — re-run for this settlement through `dist/bin.js`: the
late veto leaves `approved/request.txt` ABSENT and journals `compensated` (Repros).

**The compiler — §A.95, §A.94, §A.98, §A.88, §A.77, §A.86 (Lane C).** Two guards made QUIETER, each
with its dropped refusals enumerated against the base: §A.95 (187 `IN_LOOP` drops over 3,000 seeded
graphs × 12 runs, 0 violations) and §A.94 (1,606 `GRAPH010` drops, 0 violations). §A.95's row
premise was false for a `conditional` exit — a `take` bypasses `when` — so that shape stays refused.
§A.98's row was wrong about its cause (the check existed, below a `continue`). §A.77 and §A.86 were
the maintainer's Q11 and Q12.

**The install surface — §H.16–§H.19, §A.91 (Lane D).** `pack.mjs` packs `git archive HEAD`, strips
the map pointers, refuses cleanly; the OTLP scope is `@caohaotiantian/loom/telemetry`; the `.d.ts`
need no `@types/node`; the flag doors refuse, including `loom --help --tokne x` (N7). §A.91: one
resolver for every by-hash verb. **This is the merge that unblocks the publish.**

**Item 24's first task (Lane E).** The refusal is a command:
`node --test packages/core/test/evolution/a29-verifier-certified-refused.test.ts` → 2 pass, a
green-is-wrong pin — a candidate its own verifier certifies 30/30 is refused at Δ -33.3pp, and a
control that really drops items gets the identical verdict. §A.29 stays open on Q6.

Seventeen rows struck (`TODO.md`, each with its shas and a repro): §A.77, §A.83, §A.85, §A.86,
§A.88, §A.91, §A.94–§A.99, §G.5, §H.16–§H.19. §A.90 and §H.20, closed last wave, now also have §Z
lines.

---

## 3 · What opened

Thirty-three rows, `TODO.md` §A.101–§A.127 and §H.21–§H.26, and one of them withdrawn. Thirty-two
are the lanes' residue, filed from the orchestrator's record; the rows say which were re-run here.

- **§A.101 — HIGH, kernel, re-run.** A `task.ready` landing after a Task committed re-readies it
  under the same Task id (`projection.ts`'s `task.ready` arm upserts `"ready"` unconditionally;
  `#activate` emits one per edge). `a→b` beside `a→c→c2→c3→b` → `b` ran twice, one trace span. Also:
  both arms of a fail-then-succeed node ran; a reader behind an `error` edge read `{ok: true}`.
  Reproduced on `2af9716a` by two agents independently, and on `7f576a47` here. No shipped example
  reaches it. **The next wave's first lane.**
- **Lane B:** §A.102 (`replay` does not check the successor manifest), §A.103 (an old journal's
  refusal is not journaled), §A.104 (`fallbackEdge` and case `take` not checked at the door).
- **Lane A:** §A.105 (`fs.glob`/`fs.grep` already serve `truncated`, but still write their cap
  LINE into the content too — two signals to an agent — and `triage-plan.js:53` parses the line), §A.106–§A.107 and §A.115–§A.116 and §A.118 (the create-undo's edges), §A.108–§A.113
  (§A.96's edges: served redo after a rewind, rewound replay, subgraph children, old journals,
  `ok: false` and the version, positional over-refusal), §A.114 (`fs.write` to a FIFO hangs),
  §A.117 (a `readme-gaps` probe blind to `rmSync`), §A.119 (two units called `bytes`), §A.120,
  §A.121.
- **Lane C:** §A.122 (absent `subgraph.inputs`/`outputs` crash at run time, in `#runSubgraph`), §A.123 (a compile
  tightening can stop an in-flight run).
- **Lane D:** §A.124 — WITHDRAWN in this settlement's review, premise false: the sentence at
  `cli.ts:6396` is inside `if (graph === undefined)`, and driven it prints 0 times with the graph
  present, 1 when edited or deleted, where it is the right advice. §A.125 (the
  sanitiser misses bidi controls and three vectors), §A.126 (masking order; `details.searched`
  dropped), §H.21 (`loom --help 1` exits 0), §H.22–§H.23 (`pack.mjs`), §H.24 (the scope names an
  unexported subpath), §H.25 (`node16`/`bundler` consumers unmeasured), §H.26 (`-h`/`-v` exit 2).
- **By this settlement:** §A.127 — `grant-access.json` still ships §A.94's `merge_object`
  workaround, and its `residue-error-arm` label describes a refusal that no longer happens (re-run:
  with `replace` it compiles `ok`). And §A.100 re-measured: its repro stopped producing a failed
  compensation when §A.99 closed; a changed-bytes veto still traces `[ok]` over one.

**This settlement's own review found two false premises, both in rows whose "re-run" was a grep
rather than a drive**: §A.124 read a line and not the `if` around it, and §A.105 grepped the marker
and never looked at `details`, so it asked for a `details.truncated` the tools already carried. The
first is withdrawn and the second restated; the rows re-run by DRIVING held.

Open rows: **67 → 82**. The §A rise, 37 → 51, is the largest in `TODO.md`'s history, by the
mechanism the last two settlements named: every lane was reviewed adversarially, and a reviewer
names what a closure left.

---

## 4 · Decisions taken this wave, and the review history

**By the maintainer, 2026-09-23/24:** Q1 (rename the OTLP scope), Q2 (change the types, document
the libs), **Q3 = (a)** (`fs.restore` undoes a create, refusing if the bytes differ), Q11 (refuse a
static unfanned quorum `k > n`), Q12 (§A.86 closed by argument); **N7** — `loom --help --tokne x`
REFUSES, deliberately reversing `ad83204d`'s *"after help, so a typo still gets the list"*; Lane A a
FOURTH review round and Lane D a fourth and a FIFTH, each scoped by him.

**By the orchestrator, inside Q3, and REVERSED:** review round 1 added `ctime` to the create's
identity; round 2 dropped it as STRICTER than Q3's rule — it blocked a run's own create → overwrite
rollback. Identity is `dev` + `ino` + `nlink == 1` + digest. The cost is §A.106.

**The review history, plainly.** Lane A needed four rounds on §A.96, and each round tried a new key
for "has this undone call been made again": `compensatesSeq` against the lease, then the lease, then
any performed call, then the POSITION `<taskId>:tool:<n>` — and a redo that put a different call at
the undone write's ordinal cleared the mark with the file gone. Round four bound the identity: same
position, same tool name, same `argsDigest`. Lane D needed five: its resolver's advice kept guessing
what it could not know — round four's `missingGraphAdvice` told an operator whose run file was
DELETED that "restoring changes nothing", because an unrelated candidate failed — and round five
replaced the guess with one sentence naming both cases.

**The lesson, one per lane, the same shape both times: a stand-in for a fact the code does not
have.** §A.96's four keys were each a SCALAR standing in for a per-call identity; each fix moved the
edge one step over, which is the signal a fix is mis-scoped rather than incomplete. Lane D's advice
was a sentence standing in for knowledge the resolver cannot have — whether the file was removed,
edited or never there. **When a review round reopens the same predicate one edge over, stop and
name the fact the predicate is approximating; then either obtain it or stop claiming it.** And
builders ran SUBSETS of the suite: Lane D's first tip left the full suite red — a non-owned pin in
`product-lane-doors.test.ts`, and `stranded-run-tick-cost` at 0/5 in review — so every builder report
now carries the full `npm test`.

---

## 5 · Flags for the maintainer

1. **The publish is unblocked, and it is your act** — §6, apart from the lane order. Nothing in §H.21–§H.26 is, in this
   settlement's reading, a reason to hold it.
2. **§A.106 — what Q3's rule removes.** "Refuse if the bytes differ" also removes a created file that
   was `chmod`ed, renamed away and back, hard-linked and unlinked, or rewritten with the SAME bytes by
   another run. Accept that as the rule (the row is then struck as a non-defect), or name a tighter
   identity that still lets a run's own create → overwrite roll back.
3. **§A.123 — a compile tightening against a live run.** A run begun by an older build can be refused
   mid-flight when the current build recompiles a mutation or a child subgraph. Which rules does a
   resumed run compile under?
4. **§A.111 — pre-publish journals.** A journal from before `26a4f358` (or an agent journal before
   `a2f5244a`) replays `match: false`. No journal outside this repository exists; declaring "no
   compatibility before 0.1.0" closes it by argument, and is cheapest said before the publish.
5. **Still unasked from the 2026-09-23 table:** Q4 (D9's "only in WHEN"), Q5 (item 30 closes once),
   Q6 (item 24's route — now the only thing between its repro and its build), Q7 (item 31's gate),
   Q8 (§A.82), Q9 (§A.92), Q10 (§A.93), Q13–Q20.

---

## 6 · Direction

**The publish is the maintainer's act, apart from the order** — it is unblocked, waits on no lane,
and is not a unit of lane work: remove `private: true`, `node scripts/pack.mjs --out out`,
`npm publish ./out/caohaotiantian-loom-0.1.0.tgz`, then paste the `docker run … loom --version`
receipt into item 29. It closes item 29 and §H.15, unblocks the fourth port, and triggers §H.1's
reopening.

The next wave's order is `DESIGN.md`'s ("Where it stands, 2026-09-24"):

1. **§A.101, the re-arrival kernel defect.** A defect in the executor's own contract — *a Task runs
   once* — outranks new vocabulary, and item 31 would edit the same two files.
2. **Item 31's first slice** — `durationMs` on `run.compiled`, the node type on `task.leased` —
   on Q7.
3. **Item 24's fold**, keyed on `task.leased`'s seq, once Q6 chooses it over the re-scope.
4. **The fourth port**, after the publish (Q20).

---

## 7 · Next wave — a brief sketch

**This is a sketch for the orchestrator, not a measurement.** Base: this settlement's tip. The
shared-files rule stands: no lane edits `TODO.md`, `DESIGN.md`, `CLAUDE.md`, `README.md`,
`examples/README.md` or `docs/`; one settlement lane applies every strike and row after the merges.
The lane preamble in `docs/handoff-2026-09-23.md` §7 stands, with two amendments: **every builder
report carries the FULL `npm test`**, and **a fix round that reopens the same predicate one edge over
stops and reports** (§4).

**Lane K — re-arrival, §A.101.** opus, M. *Owns:* `run/projection.ts` (KERNEL), `run/engine.ts`
(KERNEL), new `test/run/*.test.ts`. *Closes:* *"a TERMINAL Task is never re-readied (or a
re-arrival is refused, or journaled as a distinct fact), with all three shapes pinned"*. *Failing
today:* the `a→b` / `a→c→c2→c3→b` graph in Repros → `"log": ["a","c","b","c2","c3","b"]`. *Kernel:*
`fix:`; a new journal word (if re-arrival is journaled) carries a voluntary `Kernel-seam:`. *Risk:*
a join legitimately counts several arrivals — the fix must not change what `#joinArrivals` sees.
*Reviewer:* drive all three shapes on the binary; re-run §A.94's and §A.95's oracles, since their
soundness argument leans on single arrival.

**Lane L — item 31's first slice (after K merges, or on K's tip).** opus, M. *Owns:*
`journal/events.ts` (KERNEL), `run/engine.ts` (KERNEL, after K), `telemetry/spans.ts`, their tests.
*Closes:* the first two of §C.2's eight underivable attributes, name by name. *Failing today:*
`/usr/bin/grep -anc 'name: "loom\.' packages/core/src/telemetry/spans.ts` → 7, and §C.2's own
grep → 3. *Kernel:* `feat:` + `Kernel-seam:`. *Blocked on:* Q7.

**Lane M (optional, disjoint from K and L) — the examples' debts: §A.127 and §A.105.** sonnet, S.
*Owns:* `examples/graphs/grant-access.json`, `examples/resources/function/triage-plan.js`,
`builtin/tools.ts` (`fs.glob`/`fs.grep` only), `test/examples-grant.test.ts`,
`test/examples-triage.test.ts`, `test/builtin/fs-search.test.ts`. §A.105 is §A.83's lesson again:
migrate `triage-plan.js` to the projection in the same change that removes the line.

**The decision sitting — Q4–Q10, Q13–Q20, plus §5's flags 2–4.** Not a lane: the maintainer. The
table in `docs/handoff-2026-09-23.md` §7 still carries each question, its recommendation and what it
blocks; Q6 and Q7 gate lanes 4 and 3 above.

**Merge order:** K → L → M. The publish can happen at any point; nothing here changes the tarball
enough to wait for.

---

## Repros

### §A.101 — a late arrival re-runs a committed Task (`packages/core/dist/bin.js` at `7f576a47`)

Five `function` nodes, each body `(view) => ({ writes: { log: ["<its id>"] } })`, one
`append_ordered` channel `log`, edges `a→b`, `a→c`, `c→c2`, `c2→c3`, `c3→b`:

```
$ loom compile graphs/rearrival.json
ok
$ loom run graphs/rearrival.json --input '{"seed":"x"}'
  "status": "succeeded",
  "outputs": { "log": ["a", "c", "b", "c2", "c3", "b"] }
$ loom trace <runId> | /usr/bin/grep -a 'task b '
  loom.task b root [ok] 3ms
```

### §A.99 — the late veto on a fresh workspace (a fresh copy of `examples/`, one process per verb)

```
$ loom run graphs/two-person-veto.json --input '{"request":"NEW"}'   → exit=0 status=awaiting_gate
$ loom approve <runId> <gate> --as u:alice                          → exit=0 status=awaiting_gate
$ loom approve <runId> <gate> --as u:bob                            → exit=0 status=awaiting_gate
$ loom approve <runId> <gate> --as u:carol --reject                 → exit=1 status=failed
approved/request.txt after: ABSENT
journal: compensation.recorded {"compensates":"save@root#0:tool:0","outcome":"compensated","tool":"fs.write","trigger":"run_failed",…}
```

### §A.100 — the same veto with the file's bytes changed before carol's reject

```
approved/request.txt after: "EDITED"
  loom.task save root [ok] 2ms
    loom.tool (compensate) [ok] 1ms
journal: compensation.recorded {"outcome":"failed","reason":"\"fs.restore\" did not undo \"fs.write\": refusing to remove approved/request.txt: its bytes changed since the write created it, …"}
```

### The flag doors (`packages/core/dist/bin.js` at `7f576a47`, an empty directory)

```
loom --help 1                 exit=0  loom — graph-native multi-agent orchestration        ← §H.21
loom --help=yes               exit=0  loom — graph-native multi-agent orchestration        ← §H.21
loom --version 1              exit=1  E_CONFIG_INVALID: --version takes no value (got "1") …
loom --help --tokne x         exit=1  E_CONFIG_INVALID: unknown flag: --tokne (did you mean --to or --token?) …   ← N7
loom --port 1                 exit=1  E_CONFIG_INVALID: --port is read by `loom serve`. …  ← §H.19
loom --version --tokne x      exit=1  E_CONFIG_INVALID: unknown flag: --tokne …            ← §H.19
loom -h                       exit=2  unknown command "-h"                                 ← §H.26
loom -v                       exit=2  unknown command "-v"                                 ← §H.26
loom --workspace w --version  exit=0  loom 0.1.0
```

### The tarball

```
$ node scripts/pack.mjs --out $S/packout
packed @caohaotiantian/loom@0.1.0 from settle-0924@6ef3aca536d4 — 141 files, 1815 KB (5492 KB unpacked), every one compiled from a clean archive of that commit (138 sourceMappingURL pointer(s) stripped, none remain in the tarball)
! the working tree has 1 uncommitted change(s), left out of this pack (it sources settle-0924@6ef3aca536d4 only)
$ node scripts/smoke-install.mjs $S/packout/caohaotiantian-loom-0.1.0.tgz
smoke ok (tarball): installed outside the repository and ran the README's first two examples
```
