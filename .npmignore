# The published tarball ships dist/, src/, README.md, and LICENSE.
# The `files` field in package.json is the primary allowlist; this is a
# belt-and-suspenders denylist so development-only paths never ship even if
# `files` is loosened.
test/
examples/
docs/
.github/
.eagent/
*.tsbuildinfo
