{"cases":[
 {"id":"fail-open-fold","defect":true,"diff":"--- a/vocab.ts\n+++ b/vocab.ts\n@@\n export function maxPosture(...postures: readonly Posture[]): Posture {\n   let best: Posture = \"out\";\n-  for (const p of postures) if (RANK[p] !== undefined && RANK[p]! > RANK[best]) best = p;\n+  for (const p of postures) if (RANK[p] > RANK[best]) best = p;\n   return best;\n }"},
 {"id":"default-stop","defect":true,"diff":"--- a/openai.ts\n+++ b/openai.ts\n@@\n function mapFinish(reason: string): FinishReason {\n   switch (reason) {\n     case \"length\": return \"max_tokens\";\n     case \"tool_calls\": return \"tool_use\";\n-    case \"stop\": return \"stop\";\n-    default: return `unknown:${reason}`;\n+    default: return \"stop\";\n   }\n }"},
 {"id":"clear-all-actions","defect":true,"diff":"--- a/trajectory.ts\n+++ b/trajectory.ts\n@@\n     if (isEvent(e, \"task.retry_scheduled\")) {\n       s.attempts = Math.max(s.attempts, e.payload.attempt + 1);\n-      s.actions = s.actions.filter((a) => a.kind === \"gate\");\n+      s.actions.length = 0;\n       continue;\n     }"},
 {"id":"rename-local","defect":false,"diff":"--- a/projection.ts\n+++ b/projection.ts\n@@\n export function foldRun(events: Iterable<JournalEvent>): RunProjection | undefined {\n-  const all = [...events];\n-  for (const e of all) {\n+  const ordered = [...events];\n+  for (const e of ordered) {\n     apply(e);\n   }"},
 {"id":"widen-comment","defect":false,"diff":"--- a/store.ts\n+++ b/store.ts\n@@\n /**\n  * The durable write path.\n+ *\n+ * Append is a compare-and-swap on `expectedSeq`, which is what turns at-least-once task\n+ * execution into exactly-once state without a distributed lock.\n  */"},
 {"id":"add-const","defect":false,"diff":"--- a/policy.ts\n+++ b/policy.ts\n@@\n export class PolicyEngine {\n+  /** Windows are bounded so a misconfigured hold cannot stall a run forever. */\n+  static readonly MAX_WINDOW_MS = 3_600_000;\n   readonly #windows: Readonly<Record<IrreversibilityClass, number>>;"}
]}
